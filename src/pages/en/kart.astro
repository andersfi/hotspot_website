---
import Layout from "../../layouts/Layout.astro";
import ToolCard from "../../components/ToolCard.astro";
import { readFileSync } from "fs";
import { join } from "path";
import yaml from "js-yaml";

interface ToolEntry {
  id: string;
  title: { no: string; en: string };
  description: { no: string; en: string };
  iframe_src: string;
}

const base = import.meta.env.BASE_URL.replace(/\/$/, "");

const tools = yaml.load(
  readFileSync(join(process.cwd(), "src/data/tools.yaml"), "utf-8")
) as ToolEntry[];
---

<Layout title="Maps" activePage="kart" lang="en">
  <div class="page-header">
    <div class="page-header-inner">
      <h1>Map products</h1>
      <p>Interactive examples of comprehensive biodiversity maps from the Hotspots project.</p>
    </div>
  </div>

  <div class="section">
    <div class="map-list">
      {tools.map(tool => (
        <ToolCard
          title={tool.title.en}
          description={tool.description.en}
          iframeSrc={`${base}${tool.iframe_src}`}
        />
      ))}
    </div>
  </div>
</Layout>

<style>
.map-list { display: flex; flex-direction: column; gap: 2.5rem; }
</style>
